import { ContractStatusPipe } from './contract-status.pipe';

describe('ContractStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
