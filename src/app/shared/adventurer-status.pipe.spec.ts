import { AdventurerStatusPipe } from './adventurer-status.pipe';

describe('AdventurerStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AdventurerStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
